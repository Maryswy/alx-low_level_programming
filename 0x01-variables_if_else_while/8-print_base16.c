#include <stdio.h>

/**
 * main - prints numbers of base 16 in lowercase.Only use putchar
 * Return: 0
 */

int main(void)
{
	int n = 48;

	int 1 = 'a';
	while (n <= 57)
	{
		putchar(n);
		n += 1;
	}
	while (1 <= 'f')
	{
		putchar(1);
		1++;
	}
	putchar('\n');
	return (0);
}
