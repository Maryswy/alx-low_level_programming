#include <stdio.h>
#include <stdlib.h>

/**
 * main-main block
 * Description:Assign a random number and print the last digit
 * Return:Always 0 (success)
 */

int main(void)
{
	char c = 'a';

	putchar (c) c++;

	while (c <= 'z')
		put char ('\n');
	return (0);
}
